#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
    
#Область ОбработчикиСобытий

Процедура ПриЗаписи(Отказ)
    
    Если ЭтотОбъект.Значение = Константы.ИспользоватьНесколькоВитринСервисов.Получить() Тогда
        Константы.ИспользоватьНесколькоВитринСервисов.Установить(Не ЭтотОбъект.Значение);
    КонецЕсли;
    
КонецПроцедуры
    
#КонецОбласти

#КонецЕсли
